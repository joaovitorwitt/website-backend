services:
  server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - 8000:8000
    env_file:
      - ../.env
    stdin_open: true
    tty: true
    volumes:
      - ..:/app
